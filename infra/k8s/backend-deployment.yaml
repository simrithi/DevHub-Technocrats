apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: hyperadar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: hyperadar-backend:latest
          ports:
            - containerPort: 8000
